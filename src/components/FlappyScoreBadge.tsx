import styled from "styled-components";

const FlappyScoreBadge = styled.div`
  position: absolute;
  top: 106px;
  right: 16px;
  background: rgba(0, 0, 0, 0.75);
  color: #ffecb3;
  font-weight: 700;
  letter-spacing: 1px;
  padding: 8px 18px;
  border-radius: 999px;
  font-size: 1.05rem;
  pointer-events: none;
  z-index: 10;
`;

export default FlappyScoreBadge;
